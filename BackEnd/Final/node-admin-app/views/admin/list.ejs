<!--우측 상단 헤더 영역 -->
<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-10">
        <h2>관리자 정보 관리</h2>
        <ol class="breadcrumb">
            <li>
                <a href="/Home/Index">Home</a> >
            </li>
            <li>
                <a>관리자 정보 관리</a> >
            </li>
            <li class="active">
                <strong>관리자 목록 조회</strong>
            </li>
        </ol>
    </div>
    <div class="col-lg-2">

    </div>
</div>

<!--우측 콘텐츠 영역 -->
<div class="wrapper wrapper-content">

    <div class="ibox-content m-b-sm border-bottom">

        <div class="row">
            <div class="col-sm-4">
                <div class="form-group">
                    <label class="control-label" for="OrderNo">이름</label>
                    <input type="text" id="name" class="form-control">
                </div>
            </div>

            <div class="col-sm-4">
                <div class="form-group">
                    <label class="control-label" for="OrderName">IP</label>
                    <input type="text" id="ip_address" class="form-control">
                </div>
            </div>

            <div class="col-sm-4">
                <div class="form-group">
                    <label class="control-label" for="MobileNo">게시 여부</label>
                    <select id="display" class="form-control">
                        <option value="9" selected>전체</option>
                        <option value="1" selected>게시</option>
                        <option value="0" selected>게시하지 않음</option>
                    </select>
                </div>
            </div>
        </div>

        <div class="text-center">
            <button type="button" id="btnSearch" class="btn btn-primary">조회</button>
            <!-- 도메인 주소 생략 -->
            <a href="/admin/create" class="btn btn-info">신규 관리자 등록</a>
        </div>

    </div>

    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-content">
                    <div class="table-responsive">

                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th width="10%">관리자 번호</th>
                                    <th width="50%">이름</th>
                                    <th width="5%">IP</th>
                                    <th width="5%">조회 수</th>
                                    <th width="10%">게시 여부</th>
                                    <th width="10%">등록자 번호</th>
                                    <th width="10%">등록 일시</th>
                                </tr>
                            </thead>


                            <tbody class="hoverTblBody" id="list">

                            <% for(var i=0; i<admins.length; i++) { %>
                                <tr>
                                    <td><%=admins[i].admin_id%></td>
                                    <td><a href="/admin/modify/<%=admins[i].admin_id%>"><%=admins[i].name%></a></td>
                                    <td><%=admins[i].ip_address%></td>
                                    <td><%=admins[i].view_cnt%></td>
                                    <td><%=admins[i].display%></td>
                                    <td><%=admins[i].register_id%></td>
                                    <td><%=admins[i].register_date%></td>
                                </tr>
                            <% } %>

                            </tbody>
                        </table>

                    </div>
                </div>
            </div>
        </div>

    </div>


</div>